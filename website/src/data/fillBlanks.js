// Deprecated: fill-in-the-blank content now lives in Supabase (`questions`
// table, type = 'fill-blank'), fetched at runtime via src/lib/questionsApi.js.
// This export is kept only so nothing breaks if some old code still imports
// it — it's intentionally empty. Edit fill-blank questions at /admin.

export const fillBlanks = {}
